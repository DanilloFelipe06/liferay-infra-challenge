# EXAMPLE ONLY — do not apply this file as-is and never commit real credentials.
#
# Generate the real Secret imperatively instead, e.g.:
#
#   kubectl create secret generic db-credentials \
#     --namespace posts-api \
#     --from-literal=TYPEORM_USERNAME=<user> \
#     --from-literal=TYPEORM_PASSWORD=<password> \
#     --from-literal=TYPEORM_DATABASE=<database> \
#     --from-literal=MYSQL_ROOT_PASSWORD=<root-password>
#
# or manage it via a secrets manager (Sealed Secrets, External Secrets
# Operator + AWS Secrets Manager/Vault, SOPS, etc) in a real environment.
apiVersion: v1
kind: Secret
metadata:
  name: db-credentials
  namespace: posts-api
type: Opaque
stringData:
  TYPEORM_USERNAME: test
  TYPEORM_PASSWORD: CHANGE_ME
  TYPEORM_DATABASE: posts
  MYSQL_ROOT_PASSWORD: CHANGE_ME
